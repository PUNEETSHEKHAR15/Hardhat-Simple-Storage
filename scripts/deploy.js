// import section // ethers is used to interact with the contracts and network 
// main function 
// calling main function 
async function main() {

}

main().then(() => process.exit(0))
  .catch((error) => {
    console.log(error);
    process.exit(1);
  })